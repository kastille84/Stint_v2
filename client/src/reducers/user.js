

const initialState = {
  isAuth: false,
  userData: [],
  fetching: false,
  apiError: null
}


const user = (state=initialState, action) => {
  // switch(action.type) {

  // }
  return state;
}

export default user;